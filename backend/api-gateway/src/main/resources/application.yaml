spring:
  application:
    name: api-gateway

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:hlrms_redis_password}
      database: ${REDIS_DATABASE:1}
      connect-timeout: 2s
      timeout: 2s
  
  lifecycle:
    timeout-per-shutdown-phase: 30s

  cloud:
    gateway:
      server:
        webflux:
          routes:
            - id: request-service-admin-route
              uri: ${REQUEST_SERVICE_URL:http://localhost:8080}

              predicates:
                - Path=/api/v1/admin/requests,/api/v1/admin/requests/**

              filters:
                - AddResponseHeader=X-Gateway-Service,api-gateway
                
            - id: auth-service-route
              uri: ${AUTH_SERVICE_URL:http://localhost:8081}

              predicates:
                - Path=/api/v1/auth,/api/v1/auth/**

              filters:
                - AddResponseHeader=X-Gateway-Service,api-gateway

            - id: request-service-route
              uri: ${REQUEST_SERVICE_URL:http://localhost:8080}

              predicates:
                - Path=/api/v1/requests,/api/v1/requests/**

              filters:
                - name: RequestRateLimiter
                  args:
                    key-resolver: "#{@authenticatedUserKeyResolver}"
                    redis-rate-limiter.replenishRate: 10
                    redis-rate-limiter.burstCapacity: 20
                    redis-rate-limiter.requestedTokens: 1
                - AddResponseHeader=X-Gateway-Service,api-gateway

            - id: request-service-ping-route
              uri: ${REQUEST_SERVICE_URL:http://localhost:8080}

              predicates:
                - Path=/api/v1/ping

              filters:
                - AddResponseHeader=X-Gateway-Service,api-gateway

          globalcors:
            add-to-simple-url-handler-mapping: true

            cors-configurations:
              "[/**]":
                allowed-origins:
                  - ${CORS_ALLOWED_ORIGIN:http://localhost:3000}

                allowed-methods:
                  - GET
                  - POST
                  - PUT
                  - PATCH
                  - DELETE
                  - OPTIONS

                allowed-headers:
                  - Authorization
                  - Content-Type
                  - Idempotency-Key
                  - X-Correlation-ID

                exposed-headers:
                  - Location
                  - Idempotency-Replayed
                  - X-Correlation-ID
                  - X-Gateway-Service

                allow-credentials: true
                max-age: 3600

          httpclient:
            connect-timeout: ${GATEWAY_CONNECT_TIMEOUT_MS:2000}

            response-timeout: ${GATEWAY_RESPONSE_TIMEOUT:10s}

            compression: true
    discovery:
      client:
        health-indicator:
          enabled: false
        composite-indicator:
          enabled: false

server:
  port: ${SERVER_PORT:8088}
  shutdown: graceful

management:
  endpoints:
    web:
      exposure:
        include:
          - health
          - info
          - metrics
          - prometheus
          - gateway

  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
    gateway:
      access: read-only

  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
    redis:
      enabled: false

  metrics:
    tags:
      application: ${spring.application.name}

    distribution:
      percentiles-histogram:
        "{http.server.requests}": true

      slo:
        "{http.server.requests}":
          - 50ms
          - 100ms
          - 250ms
          - 500ms
          - 1s
          - 2s
          - 5s

logging:
  pattern:
    console:
      "%d{yyyy-MM-dd'T'HH:mm:ss.SSSXXX} %5p
      [%thread] [%X{correlationId:-no-correlation-id}]
      %logger{36} - %msg%n"

  level:
    root: INFO
    com.hlrms.apigateway: INFO
    org.springframework.cloud.gateway: INFO
jwt:
  issuer: hlrms-auth-service
  secret: ${JWT_SECRET:ReplaceThisWithAVeryLongRandomSecretKeyAtLeast32Characters}